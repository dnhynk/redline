[project]
name = "redline"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["openai-agents","httpx","fastapi","uvicorn[standard]","websockets",
                "trafilatura","pypdf","pyyaml","python-dotenv"]

[project.optional-dependencies]
dev = ["pytest","pytest-asyncio"]
